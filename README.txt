Execute:

	$prolog Assignment6.pl
	?- go.
	
	This will run the 5 different cases to follow the python code.

Requirements:
	If you the library lambda class is missing may have to install.

	$prolog Assignemnt6.pl
	?- pack_install(lambda).
	#EXIT prolog after install
	$prolog Assignemnt6.pl
	?- go.

	This will install the library and the next time you open the file in prolog the code will run


	
